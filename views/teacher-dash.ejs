<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Teacher Portal | Manage</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
</head>
<body class="bg-light">
    
    <div class="container py-4">
        <header class="d-flex justify-content-between align-items-center mb-4 animate__animated animate__fadeInDown">
            <div>
                <h2 class="fw-800 text-dark mb-0">Teacher <span class="text-primary">Portal</span></h2>
                <p class="text-muted small">Manage your postings [cite: 2]</p>
            </div>
            <img src="https://ui-avatars.com/api/?name=Teacher&background=6366f1&color=fff" class="rounded-circle shadow-sm" width="45">
        </header>

        <div class="row g-3 mb-4 animate__animated animate__fadeIn">
            <div class="col-6">
                <div class="glass-card p-3 text-center border-0">
                    <h4 class="fw-bold text-primary mb-0"><%= internships.length %></h4>
                    <small class="text-muted uppercase">Active Posts</small>
                </div>
            </div>
            <div class="col-6">
                <div class="glass-card p-3 text-center border-0">
                    <h4 class="fw-bold text-dark mb-0">
                        <%= internships.reduce((acc, curr) => acc + parseInt(curr.applicant_count), 0) %>
                    </h4>
                    <small class="text-muted uppercase">Total Apps</small>
                </div>
            </div>
        </div>

        <div class="mb-4">
            <button class="btn btn-primary w-100 py-3 fw-bold rounded-4 shadow-sm animate__animated animate__pulse animate__infinite" 
                    type="button" data-bs-toggle="collapse" data-bs-target="#postForm">
                + Post New Internship
            </button>
            <div class="collapse mt-3" id="postForm">
                <div class="glass-card p-4 border-0 shadow-lg">
                    <form action="/post-internship" method="POST"> [cite: 3]
                        <div class="row g-3">
                            <div class="col-12"><input name="title" placeholder="Title" class="form-control" required></div> [cite: 4]
                            <div class="col-12"><input name="link" placeholder="Application URL" class="form-control" required></div> [cite: 5]
                            <div class="col-6"><input name="skills" placeholder="Skills" class="form-control"></div> [cite: 6]
                            <div class="col-6"><input name="year" placeholder="Year" class="form-control"></div> [cite: 7]
                            <div class="col-6"><input name="pay" placeholder="Stipend" class="form-control"></div> [cite: 8]
                            <div class="col-6"><input type="number" name="positions" placeholder="Openings" class="form-control"></div> [cite: 9]
                        </div>
                        <button class="btn btn-gradient w-100 mt-4 py-3 fw-bold">Publish 🚀</button> [cite: 10]
                    </form>
                </div>
            </div>
        </div>

        <div class="mb-4 animate__animated animate__fadeIn">
            <input type="text" id="searchInput" class="form-control search-pill" placeholder="🔍 Search listings..."> [cite: 12]
        </div>

        <div id="listingsContainer" class="row">
            <% if (internships.length > 0) { %>
                <% internships.forEach((job, index) => { %> [cite: 13]
                    <div class="col-12 mb-3 listing-card-wrapper animate__animated animate__fadeInUp" 
                         style="animation-delay: <%= index * 0.1 %>s"
                         data-title="<%= job.title.toLowerCase() %>" 
                         [cite_start]data-skills="<%= job.skills_needed.toLowerCase() %>"> [cite: 13]
                        <div class="card listing-card border-0 shadow-sm">
                            <div class="card-body p-3">
                                <div class="d-flex justify-content-between">
                                    <h6 class="fw-bold mb-1"><%= job.title %></h6> [cite: 15]
                                    <span class="badge bg-light text-primary">📅 <%= job.created_at ? new Date(job.created_at).toLocaleDateString() : 'Today' %></span> [cite: 16, 17]
                                </div>
                                <p class="text-muted small mb-3">Skills: <%= job.skills_needed %></p> [cite: 18]
                                
                                <div class="row bg-light rounded-3 p-2 mb-3 g-0 text-center">
                                    <div class="col-6 border-end">
                                        <b class="d-block"><%= job.applicant_count %></b><small>Applied</small> [cite: 20]
                                    </div>
                                    <div class="col-6">
                                        <b class="d-block"><%= job.open_positions %></b><small>Slots</small> [cite: 22]
                                    </div>
                                </div>

                                <div class="d-flex gap-2">
                                    <a href="/applicants/<%= job.id %>" class="btn btn-primary flex-grow-1 rounded-3 fw-bold">View</a> [cite: 26]
                                    <form action="/delete-internship/<%= job.id %>" method="POST" class="m-0"> [cite: 27]
                                        <button class="btn btn-outline-danger rounded-3">🗑️</button> [cite: 29]
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="text-center py-5">
                    <h2>📝</h2>
                    <p class="text-muted">No postings yet.</p> [cite: 32, 33, 34]
                </div>
            <% } %>
        </div>
    </div>

    <div class="mobile-nav"> [cite: 35, 36]
        <a href="/teacher-dash" class="nav-item active">🏠<br>Home</a>
        <a href="/profile" class="nav-item">👤<br>Profile</a> [cite: 37]
        <a href="/" class="nav-item">🚪<br>Logout</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const searchInput = document.getElementById('searchInput'); [cite: 38]
        const cards = document.querySelectorAll('.listing-card-wrapper');

        searchInput.addEventListener('input', (e) => {
            const term = e.target.value.toLowerCase(); [cite: 38]
            cards.forEach(card => {
                const text = card.getAttribute('data-title') + card.getAttribute('data-skills');
                card.style.display = text.includes(term) ? 'block' : 'none'; [cite: 40]
            });
        });
    </script>
</body>
</html>