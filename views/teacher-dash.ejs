<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Teacher Portal | Manage</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;800&display=swap" rel="stylesheet">
</head>
<body class="mobile-bg">
    
    <div class="container py-4">
        <header class="d-flex justify-content-between align-items-center mb-4 animate__animated animate__fadeInDown">
            <div>
                <h2 class="fw-800 text-dark mb-0">Teacher <span class="text-primary">Portal</span></h2>
                <p class="text-muted small mb-0">Empowering student careers</p>
            </div>
            <img src="https://ui-avatars.com/api/?name=Teacher&background=6366f1&color=fff" class="rounded-circle shadow-sm" width="50">
        </header>

        <div class="mb-4 animate__animated animate__fadeInUp animate__delay-1s">
            <button class="btn btn-post-big w-100 shadow-lg" type="button" data-bs-toggle="collapse" data-bs-target="#postForm">
                <span class="d-block fs-4">✨ Post New Internship</span>
                <small class="opacity-75">Click to expand form</small>
            </button>
            
            <div class="collapse mt-3" id="postForm">
                <div class="glass-card p-4 border-0 shadow-lg">
                    <form action="/post-internship" method="POST">
                        <div class="row g-3">
                            <div class="col-12"><input name="title" placeholder="Internship Title" class="form-control big-input" required></div>
                            <div class="col-12"><input name="link" placeholder="Application URL" class="form-control big-input" required></div>
                            <div class="col-6"><input name="skills" placeholder="Skills" class="form-control big-input"></div>
                            <div class="col-6"><input name="year" placeholder="Year" class="form-control big-input"></div>
                            <div class="col-6"><input name="pay" placeholder="Stipend" class="form-control big-input"></div>
                            <div class="col-6"><input type="number" name="positions" placeholder="Openings" class="form-control big-input"></div>
                        </div>
                        <button class="btn btn-gradient-big w-100 mt-4 py-3 fw-bold haptic-btn">
                            PUBLISH INTERNSHIP 🚀
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <div class="d-flex align-items-center justify-content-between mb-3 animate__animated animate__fadeIn">
            <h5 class="fw-bold mb-0">Active Listings</h5>
            <div class="badge bg-primary rounded-pill px-3"><%= internships.length %> Posts</div>
        </div>

        <div class="search-container mb-4 animate__animated animate__fadeIn">
            <input type="text" id="searchInput" class="form-control search-big" placeholder="Search title or skills...">
        </div>

        <div id="listingsContainer" class="row">
            <% if (internships.length > 0) { %>
                <% internships.forEach((job, index) => { %>
                    <div class="col-12 mb-4 listing-card-wrapper animate__animated animate__fadeInUp" 
                         style="animation-delay: <%= index * 0.1 %>s"
                         data-title="<%= job.title.toLowerCase() %>" 
                         data-skills="<%= job.skills_needed.toLowerCase() %>">
                        <div class="card listing-card-big border-0 shadow-sm">
                            <div class="card-body p-4">
                                <div class="d-flex justify-content-between align-items-start mb-3">
                                    <h5 class="fw-bold text-dark mb-0"><%= job.title %></h5>
                                    <span class="date-pill">📅 <%= job.created_at ? new Date(job.created_at).toLocaleDateString() : 'New' %></span>
                                </div>
                                <p class="text-muted small mb-4"><b>Skills:</b> <%= job.skills_needed %></p>
                                
                                <div class="stats-row mb-4">
                                    <div class="stat-box">
                                        <span class="stat-num text-primary"><%= job.applicant_count %></span>
                                        <span class="stat-lab">APPLIED</span>
                                    </div>
                                    <div class="stat-box border-start">
                                        <span class="stat-num"><%= job.open_positions %></span>
                                        <span class="stat-lab">SLOTS</span>
                                    </div>
                                    <div class="stat-box border-start">
                                        <span class="stat-num text-success"><%= job.pay %></span>
                                        <span class="stat-lab">STIPEND</span>
                                    </div>
                                </div>

                                <div class="d-grid gap-3">
                                    <a href="/applicants/<%= job.id %>" class="btn btn-view-big">
                                        View Applied Students
                                    </a>
                                    <form action="/delete-internship/<%= job.id %>" method="POST" onsubmit="return confirm('Delete this post?');" class="m-0">
                                        <button type="submit" class="btn btn-outline-danger w-100 py-2 fw-bold rounded-3">
                                            Remove Listing 🗑️
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>

    <nav class="mobile-nav-fixed">
        <a href="/teacher-dash" class="nav-btn active"><span>🏠</span><br>Home</a>
        <a href="/profile" class="nav-btn"><span>👤</span><br>Profile</a>
        <a href="/" class="nav-btn"><span>🚪</span><br>Logout</a>
    </nav>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Haptic feel for mobile buttons
        document.querySelectorAll('.btn, .listing-card-big').forEach(el => {
            el.addEventListener('touchstart', () => el.style.transform = "scale(0.96)");
            el.addEventListener('touchend', () => el.style.transform = "scale(1)");
        });

        // Search logic
        const searchInput = document.getElementById('searchInput');
        const cards = document.querySelectorAll('.listing-card-wrapper');
        searchInput.addEventListener('input', (e) => {
            const term = e.target.value.toLowerCase();
            cards.forEach(card => {
                const text = card.getAttribute('data-title') + card.getAttribute('data-skills');
                card.style.display = text.includes(term) ? 'block' : 'none';
            });
        });
    </script>
</body>
</html>