<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Applications</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
    <style>
        .timeline-container { position: relative; padding-left: 20px; }
        .timeline-container::before {
            content: ''; position: absolute; left: 7px; top: 0; bottom: 0;
            width: 2px; background: #e2e8f0;
        }
        .step { position: relative; margin-bottom: 20px; padding-left: 20px; }
        .step::before {
            content: ''; position: absolute; left: -18px; top: 5px;
            width: 12px; height: 12px; border-radius: 50%; background: #6366f1;
            border: 2px solid #fff; box-shadow: 0 0 0 2px #6366f1;
        }
        .step.inactive::before { background: #cbd5e1; box-shadow: 0 0 0 2px #cbd5e1; }
    </style>
</head>
<body>
    <div class="container py-4">
        <header class="mb-4">
            <h2 class="fw-bold mb-1">Application <span class="text-primary">Status</span></h2>
            <p class="text-muted">Track your progress for applied roles</p>
        </header>

        <% if (apps.length === 0) { %>
            <div class="glass-card p-5 text-center mt-5">
                <div style="font-size: 3rem;">📂</div>
                <h5 class="mt-3">No applications yet</h5>
                <p class="text-muted">Explore internships and mark them as applied!</p>
                <a href="/student-dash" class="btn btn-gradient px-4">Browse Jobs</a>
            </div>
        <% } %>

<div class="row">
    <% apps.forEach(app => { %>
        <div class="col-12 mb-4">
            <div class="card internship-card shadow-sm border-0">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <h5 class="fw-bold mb-0"><%= app.title %></h5>

                        
                        <span class="badge rounded-pill bg-primary px-3 py-2"><%= app.status %></span>
                        <form action="/remove-application/<%= app.id %>" method="POST" onsubmit="return confirm('Remove this application from your list?');">
                            <button type="submit" class="btn-close" aria-label="Remove"></button>
                        </form>
                    </div>

                    <div class="bg-light p-3 rounded-3 mt-3">
                        <form action="/update-app-status/<%= app.id %>" method="POST">
                            <label class="small fw-bold text-muted mb-2">Update Your Progress:</label>
                            <div class="input-group">
                                <select name="status" class="form-select border-0 bg-white">
                                    <option value="Applied" <%= app.status === 'Applied' ? 'selected' : '' %>>Applied</option>
                                    <option value="Received Call/Message" <%= app.status === 'Received Call/Message' ? 'selected' : '' %>>Received Call/Message</option>
                                    <option value="Received Test Link" <%= app.status === 'Received Test Link' ? 'selected' : '' %>>Received Test Link</option>
                                    <option value="Received Interview Link" <%= app.status === 'Received Interview Link' ? 'selected' : '' %>>Received Interview Link</option>
                                    <option value="Hired" <%= app.status === 'Hired' ? 'selected' : '' %>>Hired 🎉</option>
                                </select>
                                <button type="submit" class="btn btn-primary">Update</button>
                            </div>
                        </form>
                    </div>

                    <div class="mt-3">
                        <a href="<%= app.link %>" target="_blank" class="btn btn-sm btn-link p-0">View Original Posting</a>
                    </div>
                </div>
            </div>
        </div>
    <% }) %>
</div>
    </div>

    <div class="mobile-nav">
        <a href="/student-dash" class="nav-item"><div>🔍</div>Explore</a>
        <a href="/my-applications" class="nav-item active"><div>📝</div>My Apps</a>
        <a href="/profile" class="nav-item"><div>👤</div>Profile</a>
    </div>
</body>
</html>