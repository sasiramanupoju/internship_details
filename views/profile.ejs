<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5" style="max-width: 600px;">
        <div class="card shadow border-0 p-4">
            <h2 class="mb-4">Profile Settings</h2>
            
            <form action="/update-profile" method="POST">
                <div class="mb-3">
                    <label class="form-label">Full Name</label>
                    <input type="text" name="name" class="form-control" value="<%= user.full_name %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Department</label>
                    <input type="text" name="dept" class="form-control" value="<%= user.department %>" required>
                </div>

                <% if (user.role === 'teacher') { %>
                    <div class="mb-3">
                        <label class="form-label">Designation</label>
                        <input type="text" name="designation" class="form-control" value="<%= user.designation %>">
                    </div>
                <% } else { %>
                    <div class="mb-3">
                        <label class="form-label">Year of Study</label>
                        <input type="text" name="year" class="form-control" value="<%= user.roll_number %>">
                    </div>
                <% } %>

                <button class="btn btn-primary w-100">Update Profile</button>
            </form>
            <hr>
            <a href="/<%= user.role === 'teacher' ? 'teacher-dash' : 'student-dash' %>" class="btn btn-outline-secondary w-100">Back to Dashboard</a>
        </div>
    </div>
</body>
</html>